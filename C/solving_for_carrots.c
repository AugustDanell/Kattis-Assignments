#include <stdio.h>

int main()
{   
    int rows;
    int points;
    
    scanf("%d%d", &rows, &points);
    for (int i = 0; i < rows; i++){
        char name[1000];
        scanf("%s", &name);
    }
    
    printf("%d",points);
    
    return 0;
}
